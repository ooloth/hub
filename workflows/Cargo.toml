[package]
name = "workflows"
version = "0.1.0"
edition = "2021"

[features]
private = ["clients/private"]

[dependencies]
anyhow = "1"
clients = { path = "../clients" }
domain = { path = "../domain" }
store = { path = "../store" }
tokio = { version = "1", features = ["full"] }
