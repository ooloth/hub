[package]
  edition = "2021"
  name = "domain"
  version = "0.1.0"

[dependencies]
  chrono = "0.4"
  secrecy = "0.8"
  serde = { version = "1", features = ["derive"] }
  serde_json = "1"

[dev-dependencies]
  rstest = "0.25"
  serde_json = "1"
