{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "workflows/github-issues.schema.json",
  "title": "github-issues",
  "description": "Surfaces open GitHub issues assigned to the authenticated user in a repository. Reads the repo from the parent project's 'repo' field. Credentials via GITHUB_TOKEN in .env.",
  "type": "object",
  "required": ["name"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "const": "github-issues"
    },
    "exclude_labels": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Exclude issues carrying any of these labels — e.g. 'wontfix', 'duplicate'."
    }
  }
}
