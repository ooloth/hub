{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "workflows/user-activity-gcp.schema.json",
  "title": "user-activity-gcp",
  "description": "Surfaces user activity from Google Cloud Logging. Reads gcp_project and service from the parent environment. Credentials via GCP_TOKEN in .env.",
  "type": "object",
  "required": ["name"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "const": "user-activity-gcp"
    },
    "include_users": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Only surface activity from these principals (email addresses). When set, all other users are ignored."
    },
    "exclude_users": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Exclude activity from these principals — e.g. bot service accounts."
    }
  }
}
