[package]
name = "config"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
insta = "1"
proptest = "1"
rstest = "0.25"
