[bans]
multiple-versions = "deny"
# Transitive conflicts from reqwest's TLS and getrandom's WASM targets — not controllable without upstream changes.
skip = [{ name = "core-foundation" }, { name = "wit-bindgen" }]

[advisories]
version = 2

[sources]
unknown-registry = "deny"
unknown-git = "deny"
