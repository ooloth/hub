[package]
  edition = "2021"
  name = "store"
  version = "0.1.0"

[dependencies]
  anyhow = "1"
  chrono = { version = "0.4", features = ["serde"] }
  dirs = "5"
  domain = { path = "../domain" }
  rusqlite = { version = "0.31", features = ["bundled"] }

[dev-dependencies]
  tempfile = "3"
